#!/bin/sh

# Dnf setup
sudo echo """
fastestmirror=True
max_parallel_downloads=10
defaultyes=True
keepcache=True
""" | sudo tee -a /etc/dnf/dnf.conf

sudo dnf clean dbcache

# Upgrade
sudo dnf -y upgrade